-- local ReplicatedStorage = game:GetService("ReplicatedStorage")
-- local Swarm = require(ReplicatedStorage.Agents.Swarm)
-- local AgentSwarm = Swarm.new()

-- local TaskClasses = require(ReplicatedStorage.Agents.Swarm.TaskClasses)

-- task.wait(2)

-- AgentSwarm:Init()

-- local tarefa = TaskClasses.GatherTask.new("CollectWood", {Type = "Wood", Count = 5000})
-- local tarefa2 = TaskClasses.GatherTask.new("CollectStone", {Type = "Stone", Count = 15000})
-- AgentSwarm:SetOrders(tarefa)
-- AgentSwarm:SetOrders(tarefa2)